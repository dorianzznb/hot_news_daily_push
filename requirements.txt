# 热点新闻收集与推送工具依赖项
# Hot News Daily Push Tool Dependencies

# ==========================================
# 核心HTTP和网络库 (Core HTTP & Network)
# ==========================================
requests>=2.28.0              # HTTP请求库，用于API调用和网页抓取
cloudscraper>=1.2.71           # 绕过Cloudflare保护的网页抓取

# ==========================================  
# 环境变量管理 (Environment Management)
# ==========================================
python-dotenv>=1.0.0          # 加载.env文件中的环境变量

# ==========================================
# 网页解析和内容提取 (Web Parsing & Content Extraction)
# ==========================================
beautifulsoup4>=4.12.0        # HTML/XML解析器
feedparser>=6.0.10            # RSS/Atom feed解析器
newspaper3k>=0.2.8            # 新闻文章内容提取
trafilatura>=2.0.0            # 网页文本内容提取

# ==========================================
# 日期时间处理 (Date & Time Processing)
# ==========================================
python-dateutil>=2.8.2       # 扩展的日期时间解析和处理

# ==========================================
# LangChain相关 (LangChain Framework)
# ==========================================
# 注意：项目中某些功能可能使用LangChain，但主要依赖自定义实现
langchain>=0.1.0
langchain-community>=0.0.10
langchain-core>=0.1.0
langchain-openai>=0.0.5

# ==========================================
# AI模型API集成 (AI Model API Integration)
# ==========================================
google-generativeai>=0.8.0    # Google Gemini API客户端

# ==========================================
# 开发和测试工具 (Development & Testing - Optional)
# ==========================================
# 取消注释以下行以安装开发工具:
# pytest>=7.0.0               # 测试框架
# flake8>=6.0.0               # 代码风格检查
# black>=23.0.0               # 代码格式化工具

# ==========================================
# 安装说明 (Installation Instructions)
# ==========================================
# 1. 创建虚拟环境 (推荐):
#    python -m venv venv
#    source venv/bin/activate  # Linux/Mac
#    venv\Scripts\activate     # Windows
#
# 2. 安装依赖:
#    pip install -r requirements.txt
#
# 3. 配置环境变量:
#    cp env_gemini_example.txt .env
#    # 编辑.env文件，填入真实的API密钥
#
# 4. 运行测试:
#    python tests/test_gemini_integration.py